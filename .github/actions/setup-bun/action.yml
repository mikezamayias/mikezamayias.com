name: "Setup Bun Environment"
description: "Ensure Bun is available and dependencies are installed"

inputs:
    bun-version:
        description: "Bun version to install"
        required: false
        # Pinned so a new Bun release can't change installs under us.
        # Keep in step with the version used locally.
        default: "1.3.11"
    frozen-lockfile:
        description: "Use frozen lockfile"
        required: false
        default: "true"
    skip-install:
        description: "Skip bun install (deps already present)"
        required: false
        default: "false"

runs:
    using: "composite"
    steps:
        - name: Setup Node.js
          uses: actions/setup-node@v5
          with:
              node-version: "22"

        - name: Setup Bun
          uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
          with:
              bun-version: ${{ inputs.bun-version }}

        - name: Cache Bun dependencies
          uses: actions/cache@a7833574556fa59680c1b7cb190c1735db73ebf0 # v5.0.0
          with:
              path: ~/.bun/install/cache
              key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
              restore-keys: ${{ runner.os }}-bun-

        - name: Install dependencies
          if: inputs.skip-install != 'true'
          shell: bash
          run: |
              if [ "${{ inputs.frozen-lockfile }}" = "true" ]; then
                bun install --frozen-lockfile
              else
                bun install
              fi
